name: future_bundle
description: You can now process multiple Futures at once.
version: 0.0.4
homepage: https://github.com/jinhan38/future_bundle

environment:
  sdk: '>=2.16.2 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
